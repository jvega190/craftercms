version: "~> 1.0"
git:
    depth: 1
    quiet: true
language: java
jdk:
    - openjdk21
cache:
    directories:
        - "$HOME/.gradle/caches"
        - "$HOME/.gradle/wrapper"
branches:
    only:
        - develop
        - support/4.1
notifications:
    slack: craftersoftware:7YiSb41ZG5rfMgiAY7Qgprc7
install: skip
script:
    - ./gradlew build -x test -Ptravis=true
